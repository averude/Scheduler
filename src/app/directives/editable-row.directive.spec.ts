import { EditableRowDirective } from './editable-row.directive';

describe('EditableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableRowDirective();
    expect(directive).toBeTruthy();
  });
});
