import { ScheduleLabelPipe } from './schedule-label.pipe';

describe('ScheduleLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
