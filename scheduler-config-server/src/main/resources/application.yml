spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
#  security:
#    user:
#      password: ${CONFIG_SERVER_PASSWORD}
server:
  port: 8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTRY_SERVER_URL}:${REGISTRY_SERVER_PORT}/eureka/