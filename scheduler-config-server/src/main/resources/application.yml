spring:
  application:
    name: scheduler-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: ${REGISTRY_SERVER_URL:http://localhost:8761/eureka/}