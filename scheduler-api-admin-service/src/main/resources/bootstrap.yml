spring:
  application:
    name: scheduler-api-admin-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL}
      fail-fast: true
      profile: native
#  main:
#    allow-bean-definition-overriding: true